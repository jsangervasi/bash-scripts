# Author: Joseph M Sangervasi <jsangervasi@hotmail.com>
# Title: keep alive statement for ssh sessions
# Usage (Linux/Mac): add to .bashrc,.zshrc,.profile
# Function: runs a background process which prints space to terminal window every 5 minutes to keep ssh session alive (EC2, Virtual machines, etc)
#!/bin/bash
while true;do echo " ";sleep 300;done &
